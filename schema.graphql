type Item @entity {
  id: ID!
  tokenId: BigInt!
  tokenUri: String!
  owner: User!
  contract: String!
}

type MetadataUpdateRecord @entity {
  id: ID!
  item: Item!
  actor: User!
}

type User @entity {
  id: ID!
  items: [Item!]! @derivedFrom(field: "owner")
}
