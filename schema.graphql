type Item @entity {
  id: ID!
  tokenId: BigInt!
  tokenUri: String!
  owner: User!
}

type MetadataUpdateRecord @entity {
  id: ID!
  tokenId: BigInt!
  actor: User!
}

type User @entity {
  id: ID!
  items: [Item!]! @derivedFrom(field: "owner")
}
